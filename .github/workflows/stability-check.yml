name: Stability Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Fix OCR response body handling safely
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('src/components/MyRequestsView.tsx')
          s = p.read_text(encoding='utf-8')
          old = '''    let errMessage = `Server returned ${response.status}`;

    try {
      const errJson = await response.json();
      if (errJson && errJson.error) {
        errMessage = errJson.error;
      }
    } catch {
      const text = await response.text();
      if (text) errMessage = text;
    }
    throw new Error(errMessage);'''
          new = '''    let errMessage = `Server returned ${response.status}`;

    try {
      const responseBody = await response.text();

      if (responseBody) {
        try {
          const errJson = JSON.parse(responseBody);

          if (errJson && errJson.error) {
            errMessage = String(errJson.error);
          } else {
            errMessage = responseBody;
          }
        } catch {
          errMessage = responseBody;
        }
      }
    } catch {
      // Keep HTTP status message
    }
    throw new Error(errMessage);'''
          if old in s:
              p.write_text(s.replace(old, new, 1), encoding='utf-8')
              print('OCR error-handler patch applied.')
          elif new in s:
              print('OCR error-handler patch already applied.')
          else:
              raise SystemExit('Expected OCR error-handler block was not found; stopping without modifying the source file.')
          PY

      - name: Typecheck
        run: bun run lint

      - name: Build
        run: bun run build

      - name: Commit OCR fix
        shell: bash
        run: |
          if git diff --quiet -- src/components/MyRequestsView.tsx; then
            echo "No OCR source change to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/MyRequestsView.tsx
          git commit -m "Fix OCR response body handling"
          git push origin HEAD:main
